# The port number on which the server will run
PORT = Your port number

# Google Cloud OAuth 2.0 Client ID
# Obtain this from the Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID = Your Google client ID

# Google Cloud OAuth 2.0 Client Secret
# Obtain this from the Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_SECRET = Your Google client secret

# The callback URL for Google OAuth 2.0
# This should match the redirect URI set in your Google Cloud Console
CALLBACK_URL = Your callback URL

# A strong secret key for session management
# Generate a secure key using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET = Your session secret